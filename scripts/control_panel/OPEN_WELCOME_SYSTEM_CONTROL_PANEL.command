#!/usr/bin/env bash
# Opens the Welcome System staff control panel when double-clicked in macOS Finder.
# Use this file after the one-time Mac setup has completed.
"$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/OPEN_WELCOME_SYSTEM_CONTROL_PANEL.sh"
